<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Main Page</title>
</head>
<body>
  <%- include('./partials/nav') %>
  <div class="container mt-4">
    <h1 class="text-center">Welcome to the Portfolio Platform!</h1>

    <% if (user) { %>
      <p class="text-center">Hello, <strong><%= user.role %></strong>!</p>

      <div class="d-flex justify-content-center gap-3">
        <% if (user.role === 'admin') { %>
          <a href="/portfolio/admin" class="btn btn-primary">Manage Portfolio</a>
        <% } %>
        <% if (user.role === 'admin') { %>
          <a href="/portfolio/editor" class="btn btn-primary">Editor Panel</a> 
        <% } %>
        <a href="/api/stock" class="btn btn-secondary">View Stock Data</a>
        <a href="/api/news" class="btn btn-secondary">View News Articles</a>
      </div>
    <% } else { %>
      <p class="text-center mt-4">Please <a href="/auth/login">log in</a> or <a href="/auth/register">register</a>.</p>
    <% } %>
  </div>

  <div class="d-flex justify-content-center align-items-center my-5">
    <div id="fixedCarousel" class="carousel slide w-50" data-bs-ride="carousel">
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="/uploads/R.jpg" class="d-block w-100 rounded" alt="Image 1">
        </div>
        <div class="carousel-item">
          <img src="/uploads/S.jpg" class="d-block w-100 rounded" alt="Image 2">
        </div>
        <div class="carousel-item">
          <img src="/uploads/T.jpg" class="d-block w-100 rounded" alt="Image 3">
        </div>
      </div>
      <button class="carousel-control-prev" type="button" data-bs-target="#fixedCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#fixedCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>
  </div>

  <%- include('./partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
